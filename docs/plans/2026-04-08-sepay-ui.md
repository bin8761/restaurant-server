# SePay UI Cleanup Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Simplify SePay admin UI by hiding unused refund/transaction fields, mapping statuses to friendly Vietnamese, formatting time in Vietnam timezone, and limiting search to Order ID.

**Architecture:** Keep all changes in FE Admin. Add small mapping/format helpers in `app/payments/page.tsx` (or a local utility) and adjust render logic to hide columns/inputs and apply mappings/formatting.

**Tech Stack:** Next.js (App Router), React, TypeScript, SWR, shadcn/ui.

---

### Task 1: Add status + time formatting helpers

**Files:**
- Modify: `Fe-Admin/app/payments/page.tsx`

**Step 1: Write the failing test**

Create a minimal helper test if a test harness exists. If no FE test harness, skip and go to Step 3.

```ts
// (Optional) Fe-Admin/__tests__/payments.format.test.ts
import { mapStatusLabel, formatVnDateTime } from "@/app/payments/page";

it("maps status to friendly label", () => {
  expect(mapStatusLabel("PENDING")).toBe("Chờ thanh toán");
});

it("formats time in Vietnam timezone", () => {
  expect(formatVnDateTime("2026-04-08T09:05:38Z")).toMatch(/\d{2}:\d{2}:\d{2} \d{2}\/\d{2}\/\d{4}/);
});
```

**Step 2: Run test to verify it fails**

Run: `npm test` (or your FE test command)
Expected: FAIL (helpers not found)

**Step 3: Write minimal implementation**

Add helpers in `Fe-Admin/app/payments/page.tsx`:

```ts
const STATUS_LABELS: Record<string, string> = {
  ALL: "Tất cả",
  PENDING: "Chờ thanh toán",
  PAID: "Đã thanh toán",
  FAILED: "Thất bại",
  EXPIRED: "Hết hạn",
  PAID_PENDING_SYNC: "Đã thanh toán (đang đồng bộ)",
};

function mapStatusLabel(status?: string): string {
  if (!status) return "Không xác định";
  return STATUS_LABELS[status] ?? "Không xác định";
}

function formatVnDateTime(value?: string | number | null): string {
  if (!value) return "—";
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return "—";
  const parts = new Intl.DateTimeFormat("vi-VN", {
    timeZone: "Asia/Ho_Chi_Minh",
    hour: "2-digit",
    minute: "2-digit",
    second: "2-digit",
    day: "2-digit",
    month: "2-digit",
    year: "numeric",
  }).formatToParts(date);
  const get = (type: string) => parts.find((p) => p.type === type)?.value ?? "";
  return `${get("hour")}:${get("minute")}:${get("second")} ${get("day")}/${get("month")}/${get("year")}`;
}
```

**Step 4: Run test to verify it passes**

Run: `npm test`
Expected: PASS

**Step 5: Commit**

```bash
git add Fe-Admin/app/payments/page.tsx

git commit -m "feat(sepay): add status and time mapping helpers"
```

---

### Task 2: Hide refund/manual UI and Transaction Ref column

**Files:**
- Modify: `Fe-Admin/app/payments/page.tsx`

**Step 1: Write the failing test**

If UI snapshot tests exist, add an expectation that the refund note input and refund action column are absent.

**Step 2: Run test to verify it fails**

Run: `npm test`
Expected: FAIL

**Step 3: Write minimal implementation**

In the page component:
- Remove/skip rendering of:
  - Transaction Ref column header/cells
  - “Hoàn tiền thủ công” column
  - “Thao tác” column
  - Input “Ghi chú hoàn tiền thủ công (tùy chọn)”
  - “Theo dõi giao dịch ...” description line

**Step 4: Run test to verify it passes**

Run: `npm test`
Expected: PASS

**Step 5: Commit**

```bash
git add Fe-Admin/app/payments/page.tsx

git commit -m "chore(sepay): hide refund and transaction ref UI"
```

---

### Task 3: Search only by Order ID and remove placeholder

**Files:**
- Modify: `Fe-Admin/app/payments/page.tsx`

**Step 1: Write the failing test**

Add a small test for the filter logic that ensures `#123` and `123` both match `order_id = 123`.

**Step 2: Run test to verify it fails**

Run: `npm test`
Expected: FAIL

**Step 3: Write minimal implementation**

- Remove placeholder text from the search input.
- Search logic: normalize input by stripping `#` and whitespace. Match only `order_id`.

```ts
const normalizedQuery = query.trim().replace(/^#/, "");
const matchesOrder = !normalizedQuery || String(tx.order_id) === normalizedQuery;
```

**Step 4: Run test to verify it passes**

Run: `npm test`
Expected: PASS

**Step 5: Commit**

```bash
git add Fe-Admin/app/payments/page.tsx

git commit -m "feat(sepay): restrict search to order id"
```

---

### Task 4: Apply status mapping and VN time formatting in UI

**Files:**
- Modify: `Fe-Admin/app/payments/page.tsx`

**Step 1: Write the failing test**

Add a simple test that a `PENDING` status renders “Chờ thanh toán” and time is formatted as `HH:mm:ss dd/MM/yyyy`.

**Step 2: Run test to verify it fails**

Run: `npm test`
Expected: FAIL

**Step 3: Write minimal implementation**

- Replace status badge text with `mapStatusLabel(tx.status)`.
- Replace time cell text with `formatVnDateTime(tx.created_at || tx.createdAt || tx.updated_at)` (choose actual field used).
- Status filter tabs display mapped labels (using STATUS_LABELS for titles).

**Step 4: Run test to verify it passes**

Run: `npm test`
Expected: PASS

**Step 5: Commit**

```bash
git add Fe-Admin/app/payments/page.tsx

git commit -m "feat(sepay): map status labels and format time"
```

---

### Task 5: Manual verification

**Files:**
- Modify: `Fe-Admin/app/payments/page.tsx`

**Step 1: Run app**

Run: `npm run dev` (in `Fe-Admin`)

**Step 2: Verify**
- Transaction Ref / Hoàn tiền thủ công / Thao tác / Ghi chú / mô tả “Theo dõi giao dịch...” đã ẩn.
- Status chip hiển thị văn nói.
- Thời gian đúng múi giờ Việt Nam và format `HH:mm:ss dd/MM/yyyy`.
- Search chỉ lọc theo Order ID, hỗ trợ `#123` và `123`.

**Step 3: Commit (if any)**

```bash
git add Fe-Admin/app/payments/page.tsx

git commit -m "chore(sepay): verify ui adjustments"
```
